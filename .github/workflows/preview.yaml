name: preview-images

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    env:
      COMMIT_SHA: ${{ github.event.pull_request.head.sha }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build and push API
        uses: docker/build-push-action@v5
        with:
          context: services/api
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/prod-preview-api:${{ env.COMMIT_SHA }}

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: services/frontend
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/prod-preview-frontend:${{ env.COMMIT_SHA }}

      - name: Build and push Worker
        uses: docker/build-push-action@v5
        with:
          context: services/worker
          push: true
          tags: ${{ env.DOCKERHUB_USERNAME }}/prod-preview-worker:${{ env.COMMIT_SHA }}

      - name: Comment preview instructions
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            Preview deploying for commit `${{ env.COMMIT_SHA }}`. Check namespace `pr-${{ github.event.pull_request.number }}`
